# Используйте официальный образ Python
FROM python:3.9

# Установите рабочую директорию в контейнере
WORKDIR /app

# Скопируйте статические файлы в контейнер
COPY . /app

# Запустите HTTP сервер на порту 8000
CMD ["python", "-m", "http.server", "8000"]
